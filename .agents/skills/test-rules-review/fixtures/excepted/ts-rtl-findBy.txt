import { render, screen } from '@testing-library/react'

test('shows the session nickname once loaded', async () => {
  render(<SessionCard id="agent-1" />)

  const name = await screen.findByText('agent-1')
  expect(name).toBeVisible()
})
