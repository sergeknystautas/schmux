import { test, expect } from '@playwright/test'

test('debounced search settles on final results', async ({ page }) => {
  await page.getByPlaceholder('Search').fill('schmux')

  await expect.poll(async () => searchResultCount(page), { timeout: 5000 }).toBe(3)
})
