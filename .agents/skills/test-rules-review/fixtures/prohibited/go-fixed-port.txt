package demo_test

import (
	"net"
	"testing"
)

func TestDashboardServes(t *testing.T) {
	ln, err := net.Listen("tcp", ":7337")
	if err != nil {
		t.Fatal(err)
	}
	defer ln.Close()
	// ... serve and request
}
